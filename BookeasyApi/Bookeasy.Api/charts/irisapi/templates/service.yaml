apiVersion: v1
kind: Service
metadata:
  name: {{ template "irisapi.fullname" . }}
  labels:
    app: {{ template "irisapi.name" . }}
    chart: {{ template "irisapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "irisapi.name" . }}
    release: {{ .Release.Name }}
